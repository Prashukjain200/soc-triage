[project]
name = "soc-triage"
version = "0.1.0"
description = "Security Alert Triage Agent — multi-signal correlation with a human gate on dismissal"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
